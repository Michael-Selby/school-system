spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/school_system   # <-- change to your DB
    username: postgres                               # <-- your username
    password: NewPost                   # <-- your password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9080